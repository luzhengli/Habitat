---
design_system:
  name: Habitat Quiet Native
  version: 1.0.0
  status: approved-direction
  updated: "2026-08-09"
  product: Habitat
  platform: macOS desktop via Tauri
  theme: light
  visual_target: "方案 1：静谧原生"

personality:
  keywords:
    - 安静
    - 原生
    - 精确
    - 可信
    - 克制
  statement: "像一件长期使用的优秀 Mac 工具，而不是套用暗色科技模板的 SaaS 面板。"

color:
  canvas: "#FAFAF8"
  surface_primary: "#FFFFFF"
  surface_sidebar: "#F6F6F3"
  surface_subtle: "#F3F3F0"
  surface_selected: "#F1EFEC"
  text_primary: "#202124"
  text_secondary: "#686C72"
  text_muted: "#8A8F96"
  text_inverse: "#FFFFFF"
  border_default: "#E2E3E0"
  border_strong: "#D2D4D1"
  accent: "#FF5A49"
  accent_hover: "#E94B3C"
  accent_pressed: "#D94134"
  accent_subtle: "#FFF0ED"
  success: "#42A85A"
  success_subtle: "#EAF6EC"
  warning: "#B77924"
  warning_subtle: "#FFF5E5"
  danger: "#C83E36"
  danger_subtle: "#FCECEB"
  focus_ring: "rgba(255, 90, 73, 0.28)"
  overlay: "rgba(32, 33, 36, 0.20)"

typography:
  family_ui: '-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Helvetica Neue", sans-serif'
  family_display: '-apple-system, BlinkMacSystemFont, "SF Pro Display", "PingFang SC", sans-serif'
  family_mono: '"SFMono-Regular", "SF Mono", Menlo, Consolas, monospace'
  weight_regular: 400
  weight_medium: 500
  weight_semibold: 600
  size_caption: "12px"
  size_body_small: "13px"
  size_body: "14px"
  size_heading_small: "16px"
  size_heading: "20px"
  size_screen_title: "24px"
  line_height_compact: 1.25
  line_height_body: 1.5
  line_height_relaxed: 1.65

spacing:
  unit: "4px"
  xs: "4px"
  sm: "8px"
  md: "12px"
  lg: "16px"
  xl: "20px"
  2xl: "24px"
  3xl: "32px"
  4xl: "40px"

radius:
  control_small: "6px"
  control: "8px"
  panel: "10px"
  round: "999px"

border:
  hairline: "1px"
  selected_edge: "2px"

shadow:
  control: "0 1px 2px rgba(32, 33, 36, 0.06)"
  popover: "0 12px 32px rgba(32, 33, 36, 0.14)"
  window: "0 24px 70px rgba(32, 33, 36, 0.18)"

motion:
  fast: "120ms"
  normal: "180ms"
  easing: "cubic-bezier(0.2, 0.8, 0.2, 1)"

layout:
  reference_viewport: "1440x1024"
  sidebar_width: "268px"
  inspector_width: "368px"
  workspace_min_width: "700px"
  topbar_height: "100px"
  toolbar_height: "76px"
  table_header_height: "48px"
  group_header_height: "48px"
  skill_row_height: "88px"
  statusbar_height: "52px"
  content_gutter: "24px"
---

# Habitat Design System

## 1. 设计目标

Habitat 是一个本地优先的技能管理工具：维护唯一的本地技能库，通过符号链接把技能添加到不同项目，并协助用户检查来源、Git 状态和更新。

界面必须传达三件事：

1. **本地所有权**：技能始终来自用户控制的本地仓库。
2. **操作可预期**：添加技能只是创建符号链接，不复制或隐式修改源文件。
3. **长期可用**：它应像成熟的原生工具，安静、紧凑、稳定，而不是追逐短期“科技感”。

视觉方向为「静谧原生」：暖白背景、石灰色分区、炭黑文字和单一珊瑚红强调色。主要层级依靠排版、对齐、留白和细分隔线，而不是卡片、渐变或阴影堆叠。

## 2. 设计原则

### 2.1 主色只标记关键动作

珊瑚红 `#FF5A49` 只用于：

- 当前项目的窄选中标记；
- “添加到 {project}”等当前流程的主操作；
- 可执行的添加图标；
- 必须引起注意、但尚未构成错误的局部状态。

普通链接、已完成状态、工具栏按钮和装饰元素不得争用主色。一个视区内原则上只允许一个实心主按钮。

### 2.2 用结构代替卡片

- 三栏是工作区，不是三组悬浮卡片。
- 表格行通过留白和 `1px` 分隔线区分。
- 只有路径、代码、可复制值等需要明确边界的内容使用轻量容器。
- 阴影仅用于浮层、菜单、对话框和窗口，不用于常驻信息块。

### 2.3 信息密度高，但不拥挤

- 关键数据横向对齐，减少重复标签。
- 每个技能行只显示名称、说明、来源、链接状态、更新状态和时间。
- 次要解释使用 `13px` 与次级文字色；不得缩小到难以阅读。
- 每个分组标题清楚表达“相对于当前项目”的状态。

### 2.4 原生感来自行为一致性

- 使用 macOS 系统字体、标准光标、键盘焦点和熟悉的窗口结构。
- 表格选择、菜单、搜索、复制、刷新和 Finder 跳转应符合桌面应用预期。
- 不模拟玻璃、金属、霓虹或科幻仪表盘。

### 2.5 设计决定必须有理由

新增 token 或组件规则时，需要说明它解决的场景。不要为了“更丰富”引入第二主色、更多圆角层级或新的阴影。

## 3. 色彩使用

### 3.1 表面层级

| 层级 | Token | 用途 |
| --- | --- | --- |
| 应用画布 | `canvas` | 中央工作区和窗口底色 |
| 主表面 | `surface_primary` | 检查器、输入框、路径框 |
| 侧栏 | `surface_sidebar` | 左侧项目导航 |
| 次级表面 | `surface_subtle` | 悬停、分组标题、安静提示 |
| 选中表面 | `surface_selected` | 当前项目或当前技能的低对比背景 |

相邻常驻区域优先使用边框区分。不得用明显投影让它们漂浮起来。

### 3.2 语义色

- **成功**：最新、检查通过、仓库正常。
- **警告**：存在更新、路径需要确认、非阻断差异。
- **危险**：无效链接、冲突、操作失败或会覆盖内容。
- 语义状态必须同时包含图标或文字，不能只依赖颜色。

### 3.3 对比度

- 正文与背景至少满足 WCAG AA 4.5:1。
- 大号文字至少满足 3:1。
- 禁用态仍需可辨认，但不得看起来可点击。
- `text_muted` 只用于辅助元数据，不用于操作标签或关键路径。

## 4. 排版

- 页面标题使用 Display 字体族、`24px / 600`，不得做成营销海报。
- 区域标题使用 `16px / 600`。
- 控件和表格主文本使用 `14px / 400–500`。
- 辅助说明与元数据使用 `13px / 400`。
- 状态栏和极短标签可使用 `12px / 400–500`。
- 路径、Git hash 和命令使用等宽字体；路径允许换行，但不能任意截断关键目录名。
- 中文与英文混排不增加字距。全大写只允许极短的技术缩写。

## 5. 栅格与空间

### 5.1 三栏框架

```text
┌──────────── 268px ────────────┬──────── flexible, min 700px ────────┬──────── 368px ────────┐
│ 项目与唯一技能库               │ 当前项目的技能                       │ 所选技能详情            │
└───────────────────────────────┴─────────────────────────────────────┴───────────────────────┘
```

- 左栏与右栏宽度固定，中央区域吸收窗口变化。
- 窗口小于 `1120px` 时，右侧检查器可收起为抽屉；左栏保持至少 `220px`。
- 窗口小于 `900px` 时，不压缩三栏到不可读，应切换为侧栏 + 单主视图，并通过工具栏进入详情。
- 主内容边距为 `24px`；组件内部通常使用 `12px` 或 `16px`。

### 5.2 垂直节奏

- 顶部标题区与工具栏使用分隔线明确区分。
- 技能行高度以 `88px` 为基准；不要为了展示更多行压低到 `64px` 以下。
- 分组标题高度 `48px`，采用主文本色或语义色，不使用大色块。
- 底部状态栏固定，不能覆盖列表滚动区域。

## 6. 图标

- 使用一致的线性图标库；推荐 Lucide 或与 Tauri 前端技术栈兼容的同类库。
- 常规尺寸 `16px`，行首技能图标 `20–24px`，描边约 `1.5px`。
- 不使用 emoji、字符图标、伪 SVG 或不成体系的插画。
- 图标必须配合可访问名称；仅图标按钮需要 tooltip。
- 状态图标使用语义色，普通导航图标使用 `text_secondary`。

## 7. 核心组件

### 7.1 窗口与标题栏

- 保留 macOS 红黄绿交通灯和足够拖拽区域。
- 标题栏与内容自然衔接，不增加品牌渐变或大 Logo 区。
- 应用标识“技能管理”只在左栏顶部出现一次。

### 7.2 项目导航

项目项包含文件夹图标、项目名和本地路径。

- 默认：透明背景，主文字 `text_primary`。
- 悬停：`surface_subtle`。
- 选中：`surface_selected` + 左侧 `2px` 珊瑚红边；不得整行填充高饱和主色。
- 路径使用 `13px` 次级色，单行截断并提供完整路径 tooltip。
- “添加项目”使用安静的图标按钮，不与主流程按钮竞争。

### 7.3 唯一技能库入口

- 技能库是一个紧凑的常驻行，位于项目列表下方靠近底部。
- 显示仓库名、连接状态和可选的简短来源信息。
- 不把唯一技能库设计成占据大量空间的导航分组。
- 多个 Git remote 或上游来源属于同一技能库的“来源设置”，不能伪装成多个本地技能库。

### 7.4 页面标题与更新操作

- 左侧显示当前项目名，例如 `media`，下方解释“管理此项目已链接的技能”。
- 右侧使用次级描边按钮“检查技能库更新”。
- 刷新期间显示旋转进度图标与“正在检查…”，按钮不可重复触发。
- 更新完成后通过局部状态和时间反馈，不弹出无意义成功对话框。

### 7.5 搜索与筛选

- 搜索框和状态筛选高度 `40px`，圆角 `8px`。
- 搜索占据剩余宽度，筛选宽度约 `144px`。
- 搜索 placeholder 为“搜索技能名称…”。
- 聚焦使用 `2px` focus ring；不要改变布局尺寸。
- 清空操作只在已有输入时出现。

### 7.6 技能列表

固定列建议：

1. 技能名称与一句说明；
2. 来源与版本；
3. 当前项目链接状态与相对路径；
4. 更新状态；
5. 最后更新时间。

分组名称必须使用：

- `此项目已链接（n）`
- `可添加到此项目（n）`

禁止用含义模糊的“已启用 / 未启用”，因为技能状态是项目级链接关系，不是全局开关。

行状态：

- 悬停：`surface_subtle`。
- 选中：非常轻的 `accent_subtle` 或 `surface_selected`，必要时加左侧窄强调边。
- 已链接：成功色图标 + “已链接”，并展示项目内相对路径。
- 可添加：珊瑚红描边加号 + “未链接”；不要将整行渲染为警告色。
- 有更新：警告色图标 + “可更新”，同时展示当前版本与新版本。

### 7.7 右侧技能检查器

信息顺序固定：

1. 名称、版本、来源和更新状态；
2. 一句话说明；
3. 将添加到的项目；
4. 符号链接目标；
5. 技能库源路径；
6. Git 状态；
7. 预检结果；
8. 安全说明；
9. 主次操作。

详情区通过 `1px` 分隔线和 `20–24px` 垂直间距组织，不把每一段做成卡片。

当真实路径、Git 输出和预检结果使检查器内容超过窗口高度时，安全说明与当前主操作固定在检查器底部，正文独立滚动并预留等高空间，不能让主操作被内容覆盖。原因是 Tauri 实际状态比静态原型更长，但用户仍需持续看见操作结果和符号链接安全边界。

路径框：

- 使用 `surface_primary`、`border_default`、`8px` 圆角。
- 内容使用 `12px` 等宽字体。
- 复制按钮固定在右上角，成功后短暂显示“已复制”。
- 符号链接使用 `source → target` 语义，视觉和文案都不能造成复制文件的误解。

### 7.8 预检结果

只展示真实执行的检查，不使用虚假的“安全扫描”装饰。

建议检查项：

- 源技能目录存在；
- 目标项目目录存在；
- 目标链接不存在或可安全替换；
- 技能名称无冲突；
- 相对链接路径有效。

通过使用绿色勾选，警告使用琥珀色图标，失败使用红色错误图标。失败项必须附带可执行的解决说明。

### 7.9 按钮

**主按钮**

- 背景 `accent`，文字 `text_inverse`，高度 `48px`，圆角 `8px`。
- 仅用于当前详情的提交动作，例如“添加到 media”。
- hover 使用 `accent_hover`，pressed 使用 `accent_pressed`。
- loading 状态保留按钮宽度，显示进度与明确动词。

**次按钮**

- 白色或透明背景，`border_default` 边框，主文字色。
- 用于“预览变更”“检查技能库更新”“在 Finder 中打开”。
- 不使用主色实心背景。

**危险按钮**

- 默认使用危险色文字或描边；只有最终破坏性确认才使用实心红色。
- 删除、解除链接必须明确目标项目和技能名。

### 7.10 安全说明

“仅创建符号链接，不复制文件”放在主按钮之前，采用安静的边框提示样式。它是信任信息，不应做成醒目的营销横幅。

### 7.11 状态栏

- 左侧显示应用就绪或当前后台操作。
- 中部显示 `media 已链接 3 / 技能库共 4` 和技能库路径。
- 右侧显示最后检查时间和刷新入口。
- 使用 `12–13px` 次级文本，不抢占主要内容。

## 8. 交互状态

所有交互组件必须覆盖：

- default
- hover
- active / pressed
- keyboard focus
- selected
- disabled
- loading
- success
- warning
- error

焦点态不得只改变颜色，应使用清晰的外圈。鼠标、键盘与辅助技术触发同一套业务行为。

### 8.1 动效

- 颜色、边框和背景变化使用 `120–180ms`。
- 抽屉、菜单和检查器切换可使用 `180ms`。
- 不使用弹跳、光晕、漂浮或持续呼吸效果。
- 遵守 `prefers-reduced-motion`；减少动效时直接切换状态。

## 9. 文案与领域语言

### 使用这些表达

- 技能库
- 当前项目
- 已链接
- 可添加到此项目
- 检查技能库更新
- 添加到 {project}
- 解除链接
- 预览变更
- 源路径
- 符号链接目标

### 避免这些表达

- 全局启用 / 全局禁用
- 安装到项目（当实际行为只是创建链接时）
- 魔法同步、智能增强、一键赋能等营销化文案
- 把 Git remote、npm 来源或上游仓库称为多个“本地技能库”

文案应解释真实结果。例如：

> 将在 `media/.agents/skills/project-harness` 创建指向本地技能库的符号链接；源文件不会被复制或修改。

## 10. 禁用模式

以下模式视为设计偏差：

- 蓝紫渐变、霓虹、发光边框、玻璃拟态；
- 大面积深蓝或黑色“开发者工具”主题；
- 每个区域都使用悬浮卡片和明显阴影；
- 所有按钮、标签和筛选器都做成胶囊；
- 超大标题、营销 Hero 或装饰性数据看板；
- 用第二个高饱和色与珊瑚红竞争；
- 用颜色代替状态文字或图标；
- 用 emoji、ASCII、字符或临时 SVG 充当正式图标；
- 在没有规范时自行创造暗色主题；
- 为“丰富页面”添加无业务意义的插画、背景纹理或装饰线。

## 11. 响应式与窗口行为

Habitat 是桌面工具，优先保证 `1440×1024` 和常见 MacBook 窗口尺寸。

- `≥ 1280px`：完整三栏。
- `1120–1279px`：缩小中央 gutter，检查器保持 `340px`。
- `900–1119px`：右侧检查器改为覆盖式抽屉。
- `< 900px`：项目侧栏可收起，中央列表保持可读；详情单独显示。
- 不以横向滚动作为主要解决方案。
- 用户调整过的栏宽可以本地记忆，但必须设置合理最小值与最大值。

## 12. 可访问性

- 可点击目标最小 `32×32px`；主要按钮和输入控件高度至少 `40px`。
- 关键流程可完全通过键盘完成。
- 可见焦点顺序与视觉阅读顺序一致：侧栏 → 工具栏 → 列表 → 检查器 → 状态栏。
- 图标按钮提供可访问名称和 tooltip。
- 状态更新使用合适的 live region，但避免频繁打断。
- 表格应暴露正确的行、列和选中关系；不要用纯 `div` 丢失语义。
- 路径允许选择和复制，错误信息与对应字段建立程序化关联。

## 13. 实现约束

- 所有颜色、间距、圆角、阴影和排版值必须从本文件顶部 token 派生。
- 组件内不得散落未命名的十六进制颜色。
- 原生系统字体优先，不为装饰额外加载 Web 字体。
- UI 图标来自单一图标库。
- 浅色主题是 V1 唯一批准主题；暗色主题需要独立设计和验收，不能自动反色生成。
- Tauri 命令的 loading、success 和 error 状态必须映射到明确的界面反馈。
- 符号链接、Git 和更新状态必须来自真实数据，不允许用静态“正常”状态掩盖未知结果。

## 14. 视觉验收清单

每次重要界面修改后，在同一窗口尺寸截图并检查：

### 视觉系统

- [ ] 页面整体为暖白与石灰色，而非冷蓝或蓝黑。
- [ ] 珊瑚红只集中在当前选择和主操作。
- [ ] 没有渐变、霓虹、发光、玻璃拟态或无意义纹理。
- [ ] 常驻区域主要靠分隔线和留白组织，而非卡片墙。
- [ ] 圆角集中在 `6–10px`，没有胶囊泛滥。

### 层级与密度

- [ ] 用户一眼能看到当前项目、已链接技能和可添加技能。
- [ ] “技能库”是唯一且紧凑的入口。
- [ ] 列表列对齐稳定，版本、路径和更新时间可扫描。
- [ ] 右侧检查器的操作顺序从信息到预检再到提交。
- [ ] 底部统计明确表达“当前项目已链接数 / 技能库总数”。

### 行为与信任

- [ ] “添加到项目”明确表示创建符号链接。
- [ ] 所有预检项都对应真实检查。
- [ ] 更新、复制、预览和添加都有 loading、success、error 反馈。
- [ ] 颜色不是状态的唯一载体。
- [ ] 键盘焦点清晰、顺序合理。

### 偏差回写

验收发现新的稳定组件模式时，应将规则和原因写回本文件；不要只在单个组件中做一次性修补。
